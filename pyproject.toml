[project]
name = "advent-of-code"
version = "0.1.0"
description = ""
authors = [
    {name = "Adam Tucker",email = "a.r.tucker@hotmail.co.uk"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
